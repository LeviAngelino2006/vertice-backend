{
  "name": "vertice-backend",
  "version": "1.0.0",
  "description": "API backend para VÉRTICE",
  "main": "src/server.js",
  "scripts": {
    "dev": "node --watch src/server.js",
    "start": "npx prisma migrate deploy && node src/server.js",
    "seed": "node prisma/seed.js",
    "postinstall": "prisma generate"
  },
  "dependencies": {
    "@prisma/client": "^5.22.0",
    "bcrypt": "^6.0.0",
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^5.0.1",
    "google-auth-library": "^10.7.0",
    "helmet": "^7.2.0",
    "jsonwebtoken": "^9.0.3",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "prisma": "^5.22.0"
  }
}
